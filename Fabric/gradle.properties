# Increase the memory available to gradle.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.parallel=true

## Fabric 1.20.1 (check on https://fabricmc.net/develop)
minecraft_version=1.20.1
loader_version=0.15.7
fabric_version=0.92.0+1.20.1

## Mod properties (shared with the Forge/NeoForge lines)
mod_version=0.1.5
maven_group=uk.niknik.coedeposits
archives_base_name=coedeposits-fabric-1.20

## ── Port-dep versions — uncomment with the code-port (mirror COE's Fabric 1.20.1) ──
# create_version=0.5.1-f-build.1417
# port_lib_version=2.3.2+1.20.1
# port_lib_modules=accessors,base,entity,extensions,networking,obj_loader,tags,transfer,models,tool_actions,client_events,brewing
